package com.airline.ticket.domain.service

import com.airline.ticket.domain.model.TicketAggregate
import com.airline.ticket.domain.valueobject.SeatNumber
import org.springframework.stereotype.Service
import kotlin.random.Random

/**
 * Ticket Domain Service
 * 
 * 복잡한 비즈니스 로직과 도메인 규칙을 처리하는 도메인 서비스
 * (사용하지 않는 메서드들 제거 완료)
 */
@Service
class TicketDomainService {
    // 현재 사용되지 않는 도메인 서비스
}
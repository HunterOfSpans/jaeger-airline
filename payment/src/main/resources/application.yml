spring:
  application:
    name: payment

server:
  port: 8081

otel:
  exporter:
    otlp:
      protocol: grpc
      endpoint: http://localhost:4317
  traces:
    exporter: otlp
  logs:
    exporter: none
  metrics:
    exporter: none
